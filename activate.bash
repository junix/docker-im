#!/usr/bin/env bash
PROJ=/opt/mos/codebase/docker-im
export PATH=${PROJ}/container:${PATH}
export PATH=${PROJ}/network:${PATH}
export PATH=${PROJ}/meaxwell:${PATH}
export PATH=${PROJ}/zk:${PATH}
export PATH=${PROJ}/kafka:${PATH}
